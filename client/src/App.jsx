// App.jsx — temporary placeholder for Issue #1 (scaffolding)
// This will be replaced in Issue #5 (React Router) with a proper router setup
// For now, it just proves the dev server, Tailwind, and the build pipeline all work
const App = () => {
  return (
    <div className="min-h-screen bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 flex items-center justify-center">
      <h1 className="text-4xl font-bold">Task Board</h1>
    </div>
  )
}

export default App
