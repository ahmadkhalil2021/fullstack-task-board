// HomePage.jsx — Landing page at "/"
// Issue #5: just a placeholder. Issue #9 will add the auto-create board logic.

const HomePage = () => {
  return (
    <div className="min-h-screen bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 flex items-center justify-center">
      <p>Loading board...</p>
    </div>
  )
}

export default HomePage
